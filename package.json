{
  "name": "fps-shooter-online-root",
  "version": "1.0.0",
  "private": true,
  "description": "3D Multiplayer Browser First-Person Shooter - Foundation Layer",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix shared install && npm --prefix shared run build && npm --prefix client install && npm --prefix client run build && npm --prefix server install && npm --prefix server run build:server-only",
    "build:server": "npm --prefix shared run build && npm --prefix server run build",
    "build:client": "npm --prefix shared run build && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "start:server": "npm --prefix server run start",
    "install:all": "npm install && npm --prefix shared install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
